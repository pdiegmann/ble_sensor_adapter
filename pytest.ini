[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts =
    --cov=custom_components.ble_sensor
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
    --verbose